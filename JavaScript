import { SDK } from "https://webdraw.com/webdraw-sdk@v1";
import WebdrawTextarea from 'https://assets.webdraw.app/vue/textarea.js';

const { createApp, ref, onMounted } = Vue;

createApp({
    components: {
        'webdraw-textarea': WebdrawTextarea
    },
    setup() {
        const mobileMenuOpen = ref(false);
        const messageInput = ref(null);
        const message = ref('');
        const projects = ref([
            {
                id: 1,
                title: 'Experiência Digital',
                description: 'Aplicação web interativa',
                image: 'https://picsum.photos/800/600?random=1',
                hover: false
            },
            {
                id: 2,
                title: 'Identidade de Marca',
                description: 'Projeto de branding corporativo',
                image: 'https://picsum.photos/800/600?random=2',
                hover: false
            },
            {
                id: 3,
                title: 'UI/UX Design',
                description: 'Interface de aplicativo móvel',
                image: 'https://picsum.photos/800/600?random=3',
                hover: false
            }
        ]);

        const toggleMenu = () => {
            mobileMenuOpen.value = !mobileMenuOpen.value;
        };

        onMounted(async () => {
            const sdk = SDK;
        });

        return {
            mobileMenuOpen,
            messageInput,
            message,
            projects,
            toggleMenu
        };
    }
}).mount('#app');
